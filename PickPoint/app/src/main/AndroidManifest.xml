<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.user.pickpoint">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:textColor="#ffffff"
            android:textSize="8pt"
            android:textStyle="bold"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DeliveryActivity" />
        <activity android:name=".PickPointActivity" />
        <activity android:name=".ParcelScanInfoActivity" />
        <activity android:name=".ParcelArrangingActivity"/>
        <activity android:name=".ParcelFormalizeActivity"/>
        <activity android:name=".ParcelReceivingActivity"/>
        <activity android:name=".LoginHeadPiece"/>
        <activity android:name=".ParcelPickPointerFioActivity"/>
        <activity android:name=".ParcelReceiverFIOActivity"/>

    </application>

</manifest>
